#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

MODE="${1:-all}"

case "$MODE" in
  prototype)
    sbt "runMain contract.ValidateJourney prototype"
    ;;
  service)
    sbt "runMain contract.ValidateJourney service"
    ;;
  all)
    sbt "runMain contract.ValidateJourney all"
    ;;
  *)
    echo "Usage: ./scripts/validate-journey [prototype|service|all]"
    exit 1
    ;;
esac
